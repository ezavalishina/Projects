<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.focus.zavalishina.rssreader">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.AddingChannelActivity" />
        <activity
            android:name=".view.activities.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".view.activities.AboutActivity" />
        <activity android:name=".view.activities.ChannelsActivity" />
        <activity android:name=".view.activities.FavoriteActivity" />

        <service
            android:name=".view.NewsLoaderService"
            android:exported="false" />

        <activity android:name=".view.activities.NewsDescriptionActivity"></activity>
    </application>

</manifest>